
/*
import {express} from '../utils/pacotes';
import LoginController from '../controllers/LoginController';
const loginController = new LoginController();
const router = express.Router();

router.get('/', loginController.getLoginList);
router.get('/LOGIN=:login_id', loginController.getLogin);
router.post('/', loginController.postRegisterLogin);
router.delete('/LOGIN=:login_id', loginController.deleteLogin);
router.put('/LOGIN=:login_id', loginController.putLogin);

module.exports = router;
*/